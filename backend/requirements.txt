# Sprint 1 backend dependencies (article ingestion, cleaning, preprocessing)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
trafilatura==2.0.0
python-dotenv==1.0.1
pypdf==5.1.0
python-docx==1.1.2
python-multipart==0.0.20
sentence-transformers

# Database (PostgreSQL via async SQLAlchemy). psycopg is only loaded when a
# DATABASE_URL is configured, so the backend still runs without a database.
sqlalchemy[asyncio]==2.0.36
psycopg[binary]==3.2.3

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.25.2

# NOTE: Sprint 2 ML dependencies (SBERT embeddings, BM25 retrieval) are kept

rank-bm25

# in requirements-ml.txt so the Sprint 1 backend stays lightweight and avoids
# pulling in torch until the comparison pipeline is implemented.
