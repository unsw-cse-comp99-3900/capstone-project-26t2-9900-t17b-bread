# Sprint 1 backend dependencies (article ingestion, cleaning, preprocessing)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
trafilatura==2.0.0
python-dotenv==1.0.1
pypdf==5.1.0
python-docx==1.1.2
python-multipart==0.0.20
# Image/scanned PDF OCR -> text/Word. PyMuPDF renders pages; pytesseract wraps
# the Tesseract OCR engine (install the Tesseract binary separately on the OS).
pymupdf==1.24.14
pytesseract==0.3.13
pillow==11.0.0

# Sprint 2 comparison pipeline (SBERT embeddings, BM25 retrieval). These are
# real runtime dependencies of app/services/embedding_service.py and
# app/services/bm25_similarity_service.py, not reserved for later.
# Pinned: unpinned sentence-transformers previously resolved to 3.3.1, which
# crashes on model load on some torch/transformers combinations with
# "Cannot copy out of meta tensor; no data!". 5.6.0 is verified working.
sentence-transformers==5.6.0
rank-bm25==0.2.2

# Database (PostgreSQL via async SQLAlchemy). psycopg is only loaded when a
# DATABASE_URL is configured, so the backend still runs without a database.
sqlalchemy[asyncio]==2.0.36
psycopg[binary]==3.2.3
psycopg2-binary
