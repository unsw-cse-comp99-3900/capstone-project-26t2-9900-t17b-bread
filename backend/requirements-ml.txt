# Sprint 2 ML dependencies for the comparison pipeline.
# Install with:  pip install -r requirements-ml.txt
# (These are heavy because of torch; not needed for Sprint 1.)
sentence-transformers==3.3.1
rank-bm25==0.2.2
scikit-learn==1.6.0
numpy==2.2.1
